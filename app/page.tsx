export default function Home() {
  return (
    <h2
      style={{
        fontSize: "2.5rem",
        fontWeight: "700",
        textAlign: "center",
        marginTop: "100px",
        color: "#0f0d8b",
      }}
    >
    Frontend deployed on render via docker image
    </h2>
  );
}